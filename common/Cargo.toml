[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde     = { version = "1", features = ["derive"] }
rmp-serde = "1"                  # MessagePack
tokio     = { workspace = true } # async framing helpers
thiserror = "1"
